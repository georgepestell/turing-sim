# Turing Machine Sim

A simple turing machine simulator, with example configurations to solve various tasks.

## Compilation

To compile the `.jar` file, run:

```bash
mvn package
```

This will create a `target` directory.

## Running

To run the main application, run:

```bash
java -jar ./target/runtm.jar <config> [<tape>]
```

